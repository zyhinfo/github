<%@ page language="java" contentType="text/html; charset=UTF-8"%><%@ include file="/jsp/base/core/taglibs.jsp"%><html>	<head>		<meta http-equiv="Content-Type" content="text/html; charset=UTF-8">		<script type="text/javascript">			var grid,quer;				        //表格	        $(function (){	        	init_page();	        	var pageSize = '${pageSize}';	            grid = $("#maingrid").ligerGrid({	                height:'99%',enabledSort:false,	                columns: [	                	{ display: '编号', name: 'LINK_ID', align: 'left', width: 150  },	                	{ display: '名称', name: 'LINK_DESC', align: 'left', width: 250 },	                	{ display: '地址', name: 'LINK_URL', align: 'left', width: 430 }	                ], 	                url:"${ctx}/LogSettingAction.action?method=getLogSettingJSON",  	                pageSize:pageSize ,rownumbers:true,	                toolbar: { items: [	                	{ id:'1', text: '查询', click: itemclick, icon: 'search2' },	                	{ line: true },	                	{ id:'2', text: '增加', click: itemclick, icon: 'add' },	                	{ line: true },	                	{ id:'3', text: '修改', click: itemclick, icon: 'modify' },	                	{ line: true },	                	{ id:'4', text: '删除', click: itemclick, icon: 'delete' },	                	{ line: true },	                	{ id:'5', text: '功能说明', click: itemclick, icon: '' }	                ]}	            });	            	            $("#pageloading").hide();	        });	        			function itemclick(item){				var id = item.id;	            var text = item.text;	            if(id == '1'){		        	query=ligerDialog_open(text,$("#targetQuery"),"queryOK","","");	            }else if(id == '2'){	            	liger.get("linkDesc").setValue("");	            	liger.get("linkUrl").setValue("");	            	ligerDialog_open(text,$("#target"),"addOK","","");	            }else if(id == '3'){	            	var row = grid.getSelectedRow();            		if (!row) { warn_alert('请选择行'); return; }            		$("#linkId").val(row.LINK_ID);            		liger.get("linkDesc").setValue(row.LINK_DESC);            		liger.get("linkUrl").setValue(row.LINK_URL);            		ligerDialog_open(text,$("#target"),"editOK","","");	            }else if(id == '4'){	            	var row = grid.getSelectedRow();            		if (!row) { warn_alert('请选择行'); return; }            		$.ligerDialog.confirm('是否删除系统日志配置？', function (isOk) {            			if(isOk){		            		$.post("${ctx}/LogSettingAction.action?method=deleteLogSetting&anticache="									+ Math.floor(Math.random() * 10000), {								id : row.LINK_ID							}, function(info) {								if (info.info == undefined) {									info = eval("(" + info + ")");								}								if (info.info == 'ok') {									grid.reload();								} else {									error_alert("${ctx}","删除系统日志配置失败","编号："+row.LINK_ID);								}							});						}            		});	            }else if (id == "5"){	            	showMenuDesc(text);	            }	        }	       //查询点击确定按钮			function queryOK(item, dialog){				var gridManager = $("#maingrid").ligerGetGridManager();        		if (!gridManager) return;   				gridManager.setOptions({parms:[{name : "linkDesc",   					value:liger.get("queryLinkDesc").getValue()}]});   				gridManager.loadData(true);   				dialog.hidden();			}			//新增点击确定按钮			function addOK(item, dialog){				var param ={					linkDesc : liger.get("linkDesc").getValue(),					linkUrl : liger.get("linkUrl").getValue()				};				$.post("${ctx }/LogSettingAction.action?method=saveLogSettingAdd&anticache="						+ Math.floor(Math.random() * 10000), param, function(info) {					if (info.info == undefined) {						info = eval("(" + info + ")");					}					if (info.info == 'ok') {	            		grid.reload();	            		dialog.hidden();					} else {						error_alert("${ctx}","新增系统日志配置失败",mapToString(param));					}				});			}			//编辑点击确定按钮			function editOK(item, dialog){				var param ={					linkId : $("#linkId").val(),					linkDesc : liger.get("linkDesc").getValue(),					linkUrl : liger.get("linkUrl").getValue()				};				$.post("${ctx }/LogSettingAction.action?method=saveLogSettingEdit&anticache="						+ Math.floor(Math.random() * 10000),param, function(info) {					if (info.info == undefined) {						info = eval("(" + info + ")");					}					if (info.info == 'ok') {	            		grid.reload();	            		dialog.hidden();					} else {						error_alert("${ctx}","修改系统日志配置失败",mapToString(param));					}				});							}		</script>	</head>	<body style="overflow-x:hidden; padding:2px;">		<div class="l-loading" style="display:block" id="pageloading"></div>    	<div id="maingrid"></div>    	<div id="targetQuery" class="self-form" style="display:none;">        	<table class="self-table">    			<tr>    				<td width="90px">名称</td>    				<td><input type="text" id="queryLinkDesc" value=""></td>    			</tr>    		</table>    	</div>    	<div id="target" class="self-form" style="display:none;">    		<table class="self-table">    			<tr>    				<td width="90px">名称</td>    				<td><input type="text" id="linkDesc" value=""></td>    			</tr>    			<tr>    				<td>链接</td>    				<td><input type="text" id="linkUrl" value=""></td>    			</tr>    		</table>    	</div>    	<input type="hidden" id="linkId" value=""/>	</body></html>