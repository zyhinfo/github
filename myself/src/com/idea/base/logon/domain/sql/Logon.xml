<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE sqlMap          PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"          "http://ibatis.apache.org/dtd/sql-map-2.dtd"><sqlMap namespace="Logon">	<typeAlias alias="LogonBean" type="com.idea.base.logon.domain.LogonBean"/>	<typeAlias alias="UserInfo" type="com.idea.base.logon.domain.UserInfo"/>	<!-- 系统主界面中左侧菜单 -->	<select id="queryLeftMenu" parameterClass="java.util.HashMap" resultClass="java.util.HashMap">		SELECT MENU_ID ID,			   OWN_MENU_ID PID,			   MENU_NAME TEXT,			   MENU_DESC,			   LINK_URL URL,			   MENU_ICON ICON  		  FROM T01_SYS_MENU WHERE MENU_ID <![CDATA[<>]]> '1'  		   AND STATUS = 1  		   <dynamic>  		   	<isNotNull property="menuIds">  		   		AND MENU_ID IN ($menuIds$)  		   	</isNotNull>  		   </dynamic>  		 order by OWN_MENU_ID ,MENU_SEQ	</select>	<select id="queryUser" parameterClass="LogonBean" resultClass="UserInfo">		SELECT user_id userId, 		       user_name userName,		       user_desc userDesc, 		       logon_name logonName		  FROM T01_SYS_user		 where logon_name = #logonName#		   and password = #password#	</select>	<select id="queryOrgAndUserGroup" parameterClass="java.lang.String" resultClass="java.util.HashMap">		select org_id,usergroup_id from T01_SYS_USER_USERGROUP where user_id = #userId#	</select>	<!-- 查询下载列表 -->	<select id="queryDownFileList" parameterClass="java.util.HashMap" resultClass="java.util.HashMap">		select DOWN_ID, DOWN_DESC, DOWN_PROGRESS, DOWN_FILE_PATH, IS_DOWN		  from T01_SYS_DOWN_LOG		 where rownum <![CDATA[<]]> 21 		   and user_id = '$userId$'		   and to_char(start_date,'yyyy-MM-dd')=to_char(sysdate,'yyyy-MM-dd')		 <dynamic>		 	<isNotNull property="idParam">		 		and DOWN_ID in ($idParam$)		 	</isNotNull>		 </dynamic>	</select></sqlMap>